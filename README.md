# easyvc
A simple Git-like tool for lazy devs who want version control without the complexity.

If you want to use it (even though no one likely will), simply place "easyvc.py" in your project folder and run it in the terminal using the command "python easyvc.py".
